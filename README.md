# Cordyceps PHP

Cordyceps is a PHP package that allows users to inject code into existing code. The package is named after the "zombie
fungus" Cordyceps, which is known for its ability to manipulate the behavior of its hosts. Cordyceps offers a solution
for injecting code into existing code, enabling users to achieve their desired results. 